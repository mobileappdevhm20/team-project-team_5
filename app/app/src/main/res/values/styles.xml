<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ListHeader" parent="TextAppearance.AppCompat.Title"></style>

    <style name="TitleHeader" parent="TextAppearance.AppCompat.Title">
        <item name="android:textSize">26dp</item>
        <item name="android:gravity">center_horizontal</item>
    </style>

    <style name="ListItem" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">18dp</item>
    </style>

    <style name="RadioButton">
        <item name="android:button">@null</item>
        <item name="android:padding">10dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">18dp</item>
        <item name="android:background">@drawable/selected_color</item>
    </style>

    <style name="Button" parent="TextAppearance.MaterialComponents.Button">
        <item name="android:layout_gravity">center_horizontal</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:padding">@dimen/general_padding</item>

    </style>
</resources>
